public class reversedMethods {
	
	/* Brute Force Algorithm
	 * Time complexity - 
	 * Space complexity - 
	 * */
	public static String bruteForce(String s) {
		
		String reversed = "";
		for (int i = s.length() - 1; i >= 0; i--) {
			reversed += s.charAt(i);
		}
		return reversed;
}
	/* TODO: Create methods with more elegant solutions
	 * is there a "reverse" method in java ?
	 * is there any way to do this problem without creating a new variable ?
	 * Maybe use temp instead....
	 * Research this problem
	 * */

	
	
	
	
}